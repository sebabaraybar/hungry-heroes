import React from "react";
import ResetPassContainer from "../containers/ResetPassContainer";

const ResetPassPage = ({ token }) => {
	return (
			<ResetPassContainer token={token}/>
	);
};

export default ResetPassPage;
