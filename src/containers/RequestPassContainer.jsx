import React from 'react';
import RequestPass from '../components/domain/RequestPass/RequestPass';

const RequestPassContainer = function () {
	return (
		<RequestPass />
	);
};

export default RequestPassContainer;
